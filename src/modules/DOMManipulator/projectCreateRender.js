const Pubsub = require("pubsub.js");

// RECIEVES
//list of projects

// DOES
//create Pop up
//edit button => project list and implement old data and set set request ( dont create new Project )
//create button => project pop up should be empty ( create new Project )
//get data from pop up ( name, color, prevName)
//closes pop up

//SENDS
//sends new Project object with( name, color, prevName )

export default createEditProject.init();

//  <div class="newProjectContainer">
// <div class="newProjectWindow">
//   <input id="color" type="color" name="" id="" />
//   <input placeholder="Project Name..." type="text" />
//   <button class="newTaskSubmit">→</button>
// </div>
